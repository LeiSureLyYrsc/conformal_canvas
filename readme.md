# Conformal Canvas

# Install
```sh
sudo apt install libboost-system-dev libboost-url-dev libopencv-dev libmuparserx-dev -y
```

# Build
```sh
git clone --recursive https://github.com/quqiOnfree/conformal_canvas.git
cmake .. && cmake --build . --config Release
```
